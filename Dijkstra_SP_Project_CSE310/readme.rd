Author: Nate Cortes
Purpose: Dijkstra's shortest path algorithm

Creates graph struture and uses Dijkstra's shortest path algorithm
------------------------------------------------------------------
4 input keys:
(S) - the program stops
(R) - the program reads edge weiths from GRAPHinput.txt to build adjacency lists for the graph
(W) - the program writes the graph information to stdout
(P) - takes two parameters, s and t. The program runs Dijktra's shortest path algorithm on the graph and prints out the shortest path
------------------------------------------------------------------
