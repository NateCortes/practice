#ifndef QUEUE_H
#define QUEUE_H

class OSqueue{
 public: 
  element* head;
  element* tail;
  int size;

 private: 
  enqueue( element*);
  dequeue( element*);
  print();
  
};

OSqueue::enqueue( element* add){
  if( add){
    if( head == NULL){
      head = tail = add;
      head.next = tail.next = add;
      head.prev = tail.prev = add;
    }else{
      
    }
    size++;
  }
}

#endif
